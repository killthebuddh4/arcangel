export const getFromGrid = () => {
  // const field = {
  //   points: [] as Arc.Point[],
  //   dimensions: {
  //     y: args.grid.length,
  //     x: args.grid[0].length,
  //   },
  // };

  // for (let y = 0; y < field.dimensions.y; y++) {
  //   for (let x = 0; x < field.dimensions.x; x++) {
  //     field.points.push({
  //       x,
  //       y,
  //       z: args.grid[y][x],
  //     });
  //   }
  // }

  return null;
};

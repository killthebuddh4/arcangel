import { Expression } from "./Expression.js";
import { Lexeme } from "./Lexeme.js";

export const parse = (args: { lexemes: Lexeme[] }): Expression => {
  //

  return null as unknown as Expression;
};
